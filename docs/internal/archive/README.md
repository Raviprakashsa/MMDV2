# Docs — How to build PDFs

To convert the generated Markdown files to PDF (Windows), install Pandoc and a TeX engine (MiKTeX or TeX Live), then run:

```powershell
.\scripts\build-pdfs.ps1
```

The script will produce PDF files next to their markdown counterparts in `docs/`.
