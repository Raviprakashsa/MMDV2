# 06 — Screen Inventory

## Executive Summary
Template and initial inventory of screens. Use a crawler or manual pass to finalize the list.

### Example: Dashboard Page
- Purpose: Display KPIs and reports
- User Role Access: Business user, Admin
- Components Used: Chart, Table, Filters
- APIs Used: `/api/reports`, `/api/widgets`
- DB Interaction: `reports`, `widgets` tables
- Suggested Redesign: Server-side pagination, cached widgets

## Next steps
- Extract routes from `app/` and create a canonical list.
