
  # Design Leads Page

  This is a code bundle for Design Leads Page. The original design reference is maintained in internal design assets.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  